name: System info

permissions:
  packages: write
  contents: write

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  info:
    name: Get info
    runs-on: ubuntu-latest
    steps:
      - name: Free some disk space
        run: |
          mount
          cat /proc/swaps
          swapon -s
